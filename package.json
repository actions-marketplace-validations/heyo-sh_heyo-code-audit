{
  "name": "heyo-code-audit-monorepo",
  "private": true,
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build && bun run bundle:action",
    "bundle:action": "bun build packages/cli/src/main.ts --outfile dist/action.js --target node --format cjs --minify",
    "typecheck": "bun run --filter @heyo-sh/code-audit-core build && bun run --filter @heyo-sh/code-audit-core typecheck && bun run --filter @heyo-sh/code-audit-runtime-pi build && bun run --filter @heyo-sh/code-audit-runtime-pi typecheck && bun run --filter @heyo-sh/code-audit-reporter-github build && bun run --filter @heyo-sh/code-audit-reporter-github typecheck && bun run --filter @heyo-sh/heyo-code-audit build && bun run --filter @heyo-sh/heyo-code-audit typecheck",
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=text && bun test --coverage --coverage-reporter=lcov && node scripts/check-line-coverage.mjs",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "quality": "bun run lint && bun run typecheck && bun run test:coverage && bun run build && bun run verify:action-bundle",
    "verify:action-bundle": "git ls-files --error-unmatch dist/action.js && git diff --exit-code -- dist/action.js",
    "prepare": "husky",
    "changeset": "changeset",
    "changeset:status": "changeset status",
    "version-packages": "changeset version",
    "release:action": "node scripts/release-action.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.3",
    "@opentelemetry/api": "1.9.1",
    "@types/bun": "1.4.2",
    "@types/node": "26.6.2",
    "husky": "9.1.7",
    "prettier": "3.9.8",
    "typescript": "7.0.2"
  }
}
